
-- OPERAÇÕES COM VIEWS

CREATE TABLE CLIENTE(
    ID_CLIENTE INT PRIMARY KEY,
    NOME VARCHAR2(50),
    SEXO CHAR(1)
);

INSERT INTO CLIENTE VALUES(1007, 'MAFRA','M');

COMMIT;

SELECT * FROM CLIENTE;

CREATE OR REPLACE VIEW V_CLIENTE
AS
    SELECT ID_CLIENTE, NOME, SEXO
    FROM
    CLIENTE;
    
    
INSERT INTO V_CLIENTE VALUES(1008, 'CLARA','F');

SELECT * FROM CLIENTE;

SELECT * FROM V_CLIENTE;



CREATE OR REPLACE VIEW V_CLIENTE_RO
AS
    SELECT ID_CLIENTE, NOME, SEXO
    FROM CLIENTE
    WITH READ ONLY;


INSERT INTO V_CLIENTE_RO
VALUES(1009, 'LILIAN','F');


-- VIEW DE JOIN

CREATE OR REPLACE VIEW RELATORIO
AS
    SELECT C.NOME, C.SEXO, T.NUMERO
    FROM CLIENTE C
    INNER JOIN TELEFONE T
    ON C.ID_CLIENTE = T.ID_CLIENTE;


-- FORCE

CREATE OR REPLACE FORCE VIEW RELATORIO
AS
    SELECT NOME, SEXO, NUMERO
    FROM CLIENTE
    INNER JOIN TELEFONE
    ON IDCLIENTE = ID_CLIENTE;

SELECT * FROM RELATORIO;


CREATE TABLE TELEONE(
    ID_TELEFONE INT PRIMARY KEY,
    NUMERO VARCHAR2(10),
    ID_CLIENTE INT
);

RENAME TELEONE TO TELEFONE;


ALTER TABLE TELEFONE ADD CONSTRAINT FK_CLIENTE_TELEFONE
FOREIGN KEY(ID_CLIENTE) REFERENCES CLIENTE;


INSERT INTO TELEFONE VALUES(1, '974726780',1007);

COMMIT;

SELECT * FROM RELATORIO;


















